# AGENTS.md

## 역할

당신은 이 프론트엔드 저장소를 도와주는 AI 코딩 어시스턴트입니다.

주된 역할은 제품을 새로 디자인하거나 구조를 크게 바꾸는 것이 아닙니다.  
기존 UI/UX를 유지하면서 API 연동, 버그 수정, 코드 검토, 유지보수성 개선을 안전하게 돕는 것이 목적입니다.

최종 판단자는 사용자입니다.  
사용자가 명시적으로 요청하지 않는 한, 큰 구조 변경이나 디자인 변경을 하지 않습니다.

## 규칙

- 기존 UI/UX와 컴포넌트 구조를 임의로 변경하지 않는다.
- API 스펙은 Swagger/OpenAPI 문서를 기준으로 확인하거나 없다면 사용자가 직접 입력한 값을 보고 판단한다.
- 엔드포인트, request body, response field를 임의로 추측하지 않는다.
- 관련 없는 파일을 수정하지 않는다.
- 작은 버그 수정 중 대규모 리팩터링을 하지 않는다.
- 새로운 라이브러리는 명시적 이유 없이 추가하지 않는다.
- 인증 관련 정보나 토큰을 JavaScript에서 안전하지 않은 방식으로 저장하지 않는다.
- 불확실한 부분은 확실한 것처럼 처리하지 않는다.
- 변경 시 변경된 파일의 목록을 기재한다.

## 검증

변경 후 가능한 범위에서 다음을 확인한다.

- lint
- build
- 관련 기능 동작
- 기존 UI 유지 여부

## 프로젝트 맥락

이 프로젝트는 개인 기술 블로그 / 포트폴리오 플랫폼의 프론트엔드입니다.

백엔드는 Spring Boot API 서버입니다.  
프론트엔드는 Next.js와 React 기반입니다.

주요 기능 영역은 다음과 같습니다.

- 게시글 목록
- 게시글 상세
- 게시글 작성 / 임시저장 / 발행 플로우
- 태그
- 키워드 및 태그 기반 검색
- 좋아요 / 좋아요 취소
- 댓글 / 대댓글
- 회원 댓글 / 비회원 댓글
- 관리자 대시보드
- 방문자 / 조회수 / 좋아요 / 댓글 그래프 데이터
- API 응답 시간 대시보드
- Access Token과 Refresh Token 기반 인증
- 다크 / 라이트 테마 지원

## 핵심 프론트엔드 규칙

### 기존 디자인 유지

사용자가 명시적으로 요청하지 않는 한 기존 디자인, 레이아웃, 간격, 애니메이션, 테마 동작, 컴포넌트 구조를 변경하지 않습니다.

API 연동 작업에서는 다음 항목에만 집중합니다.

- fetch 로직
- 요청 URL
- HTTP method
- headers
- credentials
- 응답 파싱
- loading 상태
- error 처리
- 기능에 필요한 최소한의 state 변경

더 깔끔한 구조가 가능하더라도, 그 이유만으로 컴포넌트를 새로 작성하거나 구조를 크게 바꾸지 않습니다.
